export class Person {
    constructor(name, age) {
        this.name = name;
        this.age = age;
    }
}

export const sum = (x, y) => x + y;

export const mult = (x, y) => x * y;

export const good = (x) => x + "good";